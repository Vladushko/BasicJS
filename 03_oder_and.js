'use strict';
// Offis ist von 10 bis 15 geöfnet

const hours = 10;

// (hours < 10 || hours > 15) ? alert('Offis ist geschlossen.') : alert('Offis ist geöfnet.');

hours >= 10 && hours <= 15 ? alert('Offis ist geöfnet!') : alert('Offis ist geschloßen!');
